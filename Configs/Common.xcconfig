// Default build settings — checked in to git.
//
// Override any of these by creating Configs/Local.xcconfig (gitignored).
// See README for setup. Without Local.xcconfig the project signs ad-hoc and
// skips the auto-install-to-/Applications step.

CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = -
DEVELOPMENT_TEAM =
PROVISIONING_PROFILE_SPECIFIER =

// When YES, the post-build script copies the built bundle into
// /Applications/OditBridge.app after every Debug build. Leave NO for
// contributors who just want to build and run from Xcode.
ODIT_LOCAL_INSTALL = NO

#include? "Local.xcconfig"
